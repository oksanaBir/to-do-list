import React from 'react';
import styles from '../styles/title.css';

export default function Title(props) {
    return(
        <input className={styles.title} maxLength="20">
            
        </input>
    );
}
